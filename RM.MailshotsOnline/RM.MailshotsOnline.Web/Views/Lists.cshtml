@using RM.MailshotsOnline.Web.Extensions
@inherits Glass.Mapper.Umb.Web.Ui.GlassViewPage<RM.MailshotsOnline.Entities.PageModels.Lists>
@{
    Layout = "Master.cshtml";

    var campaignId = Model.Campaign == null ? null : Model.Campaign.CampaignId.ToString();
}

<script>
    var createUrl = "@Model.CreateLinkUrl",
		backUrl = "@Model.BackLinkUrl",
		backText = "@Model.BackText",
		campaignId = "@campaignId",
		manageLists = false,
        listDetailBaseUrl = "@Model.DataListBaseUrl";
</script>

<lists-component></lists-component>