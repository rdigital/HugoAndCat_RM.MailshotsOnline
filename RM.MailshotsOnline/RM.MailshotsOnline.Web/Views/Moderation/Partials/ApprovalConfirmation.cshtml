@model RM.MailshotsOnline.Entities.ViewModels.ModerationApprovalViewModel
@if (ViewBag.ErrorMessage != null)
{
    <div>@ViewBag.ErrorMessage</div>
}

@using (Html.BeginUmbracoForm("Approve", "ModerationSurface"))
{
    @Html.ValidationSummary()
    @Html.HiddenFor(m => Model.ModerationId)
    <button class="big-button">@Model.PageModel.ConfirmButtonText</button>
}